/** Hex only. Vite loads this through site.ts from vite.config.ts.
 * primary is the focus ring (blue.500). solid is button fill so white text
 * meets 4.5:1. */
export const PALETTE = {
  primary: "#3B82F6",
  solid: "#1D4ED8",
  surface: "#FFFFFF",
  canvas: "#FAF8F5",
  text: "#111827",
} as const;
